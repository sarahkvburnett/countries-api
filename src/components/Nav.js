import React from "react";
import ThemeToggle from "./ThemeToggle";

const Nav = () => {
	return (
		<nav>
			<h1 className="title">Where in the world?</h1>
			<ThemeToggle />
		</nav>
	);
};

export default Nav;
